console.log(null == 0); // --> here null is not converting to zero as equality and comparison are different 
console.log(null >= 0); // --> here null is converting to Zero there for true
console.log(null <= 0); // --> same here null is zero

